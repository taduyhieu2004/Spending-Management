spring:
  application:
    name: ${APPLICATION_NAME:spending-management}
  profiles:
    active:
      - dev
      - dev-local
  liquibase:
    change-log: classpath:db/master.xml

  datasource:
    url: ${DATASOURCE_URL:jdbc:mysql://localhost:3307/spending_management?allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:hieu112004}
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      maximum-pool-size: 10
    jpa:
      hibernate:
        ddl-auto: none
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
application:
  token:
    key: shopsport@2023
    expire-time-access-token: 86400000
    expire-time-refresh-token: 86400000
